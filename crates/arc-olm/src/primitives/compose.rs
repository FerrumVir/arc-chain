//! Higher-order composition primitives.
//!
//! These are the "glue" that makes the DAG work.
//! They compose typed operations into programs.
//! Placeholder for now — the search engine handles composition directly.

// Composition is handled by the search engine's typed enumeration.
// Each search step selects a primitive and applies it to the current state.
// The "composition" is implicit in the search path through the DAG.
