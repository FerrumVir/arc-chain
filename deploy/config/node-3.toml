# RETIRED V2 VALIDATOR TEMPLATE — NOT A DEPLOYABLE VALIDATOR CONFIGURATION.
# An approved v3 manifest must supply a generated validator identity, artifact
# checksum, canonical genesis hash, peer set, and explicit activation plan.

[rpc]
listen = "127.0.0.1:0"
eth_port = 0

[p2p]
port = 0
peers = []

[validator]
stake = 0
min_stake = 0

[storage]
data_dir = "/tmp/arc-retired-node-3"
