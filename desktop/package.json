{
  "name": "arc-node-desktop",
  "version": "0.1.0",
  "description": "ARC Chain desktop node — one-click validator",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "copy-html": "cp src/index.html src/dist/index.html"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0"
  }
}
