node_modules
dist
dist-ssr
*.local
.vite

# Tauri
src-tauri/target
src-tauri/gen
src-tauri/WixTools

# Tauri updater signing key (the public half IS committed via
# tauri.conf.json > plugins.updater.pubkey; the private key lives in
# GitHub Actions secrets in CI and only here for local dev — NEVER git).
src-tauri/.tauri-keys/*.key
src-tauri/.tauri-keys/*.key.enc

# Playwright
test-results
playwright-report
playwright/.cache

# Editor
.vscode
.idea
.DS_Store

# Logs
*.log
npm-debug.log*
