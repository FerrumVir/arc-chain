{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash|Write|Edit",
        "command": "curl -s -X POST http://localhost:4000/events -H 'Content-Type: application/json' -d '{\"project\": \"arc-chain\", \"session\": \"'\"$CLAUDE_SESSION_ID\"'\", \"event\": \"tool_use\", \"detail\": \"Tool activity\"}' > /dev/null 2>&1 || true"
      }
    ]
  }
}
